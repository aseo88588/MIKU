name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo Hello,御主 我是暗·維希爾特
    - name: Run a multi-line script
      run: |
        add=調教=嗯啊...❤️

